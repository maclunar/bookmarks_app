h2
  ' Showing bookmark:
  b = @bookmark.name

p#notice = notice
p
  dl.dl-horizontal
    dt ID
    dd = @bookmark.id

    dt Name
    dd = @bookmark.name

    dt URL
    dd = link_to @bookmark.url, @bookmark.url

    dt Domain
    dd = Domain.find(@bookmark.domain_id).domain

    dt Tags
    dd
      - if @bookmark.tag_list.any?
        = @bookmark.tag_list
      - else
        ' No tags.

= link_to 'Edit', edit_bookmark_path(@bookmark), class: "btn btn-default btn-sm"
= link_to 'Delete', bookmark_path, method: :delete, data:{confirm: "Are you sure?"}, class: "btn btn-default btn-sm"

p
  br
  = link_to 'Back', bookmarks_path, class: "btn btn-default btn-sm"